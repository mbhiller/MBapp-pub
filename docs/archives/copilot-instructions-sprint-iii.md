# Archived: Copilot Instructions (Sprint III) — Historical Only

> WARNING: This is archival and MUST NOT be treated as current repo constraints.

- Contract-first: edit `spec/MBapp-Modules.yaml` before code.
- Feature flags default OFF; allow dev header overrides only.
- Scope (Sprint III): Views/Workspaces v1 + event-dispatcher options (noop/simulate).
- No schema-breaking migrations; keep diffs surgical.
- Allowed targets this sprint: /spec, /apps/api/src/views/*, /apps/api/src/workspaces/*, /ops/smoke/*
- Deliverables: smokes passing (views:crud, workspaces:list, events:enabled-noop) + docs update.
