# MBapp Web Environment Variables

# API Gateway base URL (required)
VITE_API_BASE=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com

# Tenant routing
VITE_TENANT=DemoTenant

# Public tenant ID for anonymous/unauthenticated requests (required for public pages)
VITE_MBAPP_PUBLIC_TENANT_ID=SmokeTenant

# Dev-only: Email for /auth/dev-login (defaults to "dev@example.com")
VITE_DEV_EMAIL=dev@example.com

# Dev-only: Tenant for /auth/dev-login (defaults to VITE_TENANT)
VITE_DEV_TENANT=DemoTenant

# Feature flags for dev/nonprod testing
VITE_MBAPP_FEATURE_REGISTRATIONS_ENABLED=true
